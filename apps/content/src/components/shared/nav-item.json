{
  "collectionName": "components_shared_nav_items",
  "info": {
    "displayName": "Nav Item",
    "icon": "bulletList",
    "description": "A navigation menu item with optional dropdown children"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true
    },
    "url": {
      "type": "string"
    },
    "is_section_heading": {
      "type": "boolean",
      "default": false,
      "description": "When true this item renders as a non-clickable group heading (e.g. 'Face', 'Body') inside a megamenu dropdown."
    },
    "image": {
      "type": "media",
      "multiple": false,
      "allowedTypes": ["images"],
      "description": "Optional thumbnail shown next to the link in a megamenu (e.g. product photo)."
    },
    "children": {
      "type": "component",
      "repeatable": true,
      "component": "shared.link"
    }
  }
}
