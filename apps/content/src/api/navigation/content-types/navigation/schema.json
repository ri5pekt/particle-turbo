{
  "kind": "collectionType",
  "collectionName": "navigations",
  "info": {
    "singularName": "navigation",
    "pluralName": "navigations",
    "displayName": "Navigation",
    "description": "Site menus identified by slug. Create entries: main-nav, footer-nav, footer-legal. Nuxt fetches by identifier."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "identifier": {
      "type": "uid",
      "required": true,
      "description": "Machine name. Examples: main-nav, footer-nav, footer-legal, mobile-nav"
    },
    "label": {
      "type": "string",
      "required": true,
      "description": "Human-readable name shown in the admin list view"
    },
    "items": {
      "type": "component",
      "repeatable": true,
      "component": "shared.nav-item"
    }
  }
}
